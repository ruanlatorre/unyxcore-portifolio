service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{document} {
      allow read: if true; // Necessário para a busca manual do login
      allow write: if request.auth != null; // Apenas usuários logados gravam dados
    }
  }
}
